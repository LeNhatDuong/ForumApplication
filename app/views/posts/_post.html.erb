<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <%= post.title %>
    <h4>posted by <%= link_to post.user.name, user_path(post.user) %></h4>
  </p>
  <h2><%= pluralize(post.views, "view") %></h2>
  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

</div>
